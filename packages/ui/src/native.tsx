import { Text, View } from 'react-native';

    // This is a placeholder to ensure the native build entrypoint is valid.
    const NativePlaceholder = () => (
      <View>
        <Text>DirectDrive UI - Native</Text>
      </View>
    );

    export default NativePlaceholder;
